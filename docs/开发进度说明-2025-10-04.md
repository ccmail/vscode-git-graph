# 开发进度说明（2025-10-04）

本说明用于同步近期开发进展、已实现功能与未完项，便于后续协同与验收。

## 进度摘要
- 完成 Panel 模式的布局与交互重构，消除顶部/右侧空白，控制栏支持左/右/上位置切换。
- Panel 中提交详情卡死（Loading）问题已修复。
- 状态栏入口与视图打开位置保持一致；新增状态栏一键终端。
- 版本已升级并打包 VSIX（1.30.1）。

## 已完成
- 设置与国际化
  - 新增设置：`git-graph.panel.controlsPosition`（Top | Left | Right，默认 Right）。
  - 为新增设置补充 NLS 文案（英文与中文）。
- Panel 布局与样式
  - 侧栏重构为贴边固定的窄栏（图标纵向排列，hover 增强）。
  - 收纳“Branches 下拉/Show Remote Branches 复选”为侧栏图标（分支/眼睛）。
  - 修复顶部空白与右侧大空白问题（滚动/定位不再叠加顶部高度；内容区仅为侧栏预留必要边距）。
- Panel 交互修复
  - 实现 `commitDetails` / `compareCommits` / `viewDiff*` / `viewFileAtRevision` 等消息处理，解决提交详情无法加载的问题。
- 状态栏改进
  - 状态栏“Git Graph”入口遵循 `git-graph.viewLocation` 设置（editor/panel 一致）。
  - 新增“终端”按钮，支持一键打开当前仓库终端，多仓库弹出选择。
- 打包与验收
  - 升级版本至 `1.30.1`，生成 `git-graph-1.30.1.vsix` 供本地安装验证。

## 未完成 / 待定
- 侧栏“更多”菜单（可选）：将无图标或低频控件进一步收纳，或支持自定义图标顺序与显示。
- 细节配置：侧栏宽度、图标大小/间距的可配置化（当前为固定值：宽 44px、图标 24px、间距 12px）。
- 视图性能回归验证：大仓库场景下的绘图与详情渲染优化未专项评估。
- 截图/演示补充：文档中未附正式截图；后续可按发布前要求补齐。

## 使用说明（要点）
- 位置一致性：
  - `git-graph.viewLocation` 设为 `panel` 时，状态栏/命令入口均在 Panel 打开；设为 `editor` 时在标签页打开。
- Panel 控制栏位置：
  - `git-graph.panel.controlsPosition` 取值 `Top/Left/Right`，默认 `Right`。
- 侧栏快捷按钮：
  - 分支（Branch 图标）：点击打开/聚焦 Branches 下拉。
  - 远程开关（Eye 图标）：切换“Show Remote Branches”。
  - 其余：查找、终端、设置、拉取、刷新。
- 打包安装：
  - 通过 VS Code “Install from VSIX…” 安装 `git-graph-1.30.1.vsix`；如遇需重启提示，请先退出编辑器后再安装。

## 兼容性与规范
- VS Code 版本要求：≥ 1.50.0（Panel/WebviewView API 依赖）。
- 代码规范：TypeScript、Tab 缩进、单引号、分号、1TBS 花括号；CRLF 换行，禁止 console/eval（使用内部日志工具）。

## 下一步建议
- 增加“更多”菜单与侧栏可配置项（顺序、宽度、图标大小）。
- 针对大仓库的性能评估与优化（渲染与详情加载）。
- 增补 CHANGELOG 截止 1.30.1 的正式条目与关键截图。

