## 主要改动
- 在 `web/main.ts` 调整分支标签渲染：恢复当前分支定位图标（白色同心圆）并保留高亮边框，同时移除描述区冗余的 `commitHeadDot`。
- 在 `web/graph.ts` 中将当前提交节点渲染为蓝色空心圆，以便在图形上直接定位 HEAD。
- `web/utils.ts` 保留定位圆圈 SVG（白色线条+实心圆点）以供复用。

## 影响范围
- Webview 提交列表中显示的当前分支徽标样式。

## 验收清单
- 当前分支标签左侧显示为“定位圆圈”样式，其余本地/远程分支仍展示 Y 形分支图标。
- 远程分支（含 `origin/HEAD`）展示为 `分支名 | 远程` 的组合，视觉风格与本地分支一致。
- 当前分支标签展示白色定位圆圈与高亮边框，远程分支（含 `origin/HEAD`）展示为 `分支名 | 远程` 的组合。
- 提交图形中仅当前提交显示蓝色空心圆，其余提交仍为实心圆。

## 已知问题
- 暂无。

## 产物
- `web/utils.ts`
- `web/main.ts`
