# 本轮目标与范围
- 完成 `web/main.ts` 中分支、远程、提交、贮藏、标签等右键菜单弹窗汉化，确保所有确认/输入对话支持中英文切换。

# 已完成
- 将分支右键推送、合并、拉取等弹窗改为 `tl()` / `t()` 调用，补充中文文案并保留英文原文（`web/main.ts`）。
- 同步调整提交、标签、贮藏、未提交更改等弹窗及运行提示、错误提示的标题内容（`web/main.ts`）。
- 调整标签详情弹窗与刷新按钮提示，保持界面语言一致（`web/main.ts`）。
- 运行 `npm run lint -- --fix`、`npm run compile`、`npm run package`，通过语法检查、编译与 VSIX 打包。

# 待解决问题
- `dialog.showError` 系列错误提示仍有英文原串，后续需集中补齐翻译。
- `handleResponsePushTagCommitNotOnRemote` 仍沿用英文逗号拼接远程列表，后续可考虑本地化格式。

# 影响范围
- Webview 侧所有右键菜单弹窗（分支/远程分支/提交/标签/贮藏/未提交更改）。
- 标签详情展示及刷新按钮提示文本。

# 验收建议
- 启动扩展后，分别触发上述右键菜单操作，确认弹窗标题、正文、按钮在中英文环境下切换正常。
- 执行 `npm run lint`、`npm run compile` 验证无新增错误。
- 检查生成的 `git-graph-1.30.4.vsix` 是否可正常安装、弹窗语言正确。

# 下一步计划
- 梳理 `dialog.showError` / `finishOrDisplayError` 相关英文提示，统一接入 `tl()`。
- 评估 `formatCommaSeparatedList` 在中文环境下的显示习惯，按需调整。

# 开发与构建（备忘）
- 关键命令：`npm run lint -- --fix`、`npm run compile`、`npm run package`。
- 主要修改文件：`web/main.ts`。
