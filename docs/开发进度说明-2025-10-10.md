## 本轮目标与范围
- 为提交列表增加“修改提交信息”能力，支持同时调整作者与提交者标识。

## 已完成
- 在 `web/main.ts` 提供上下文菜单入口，弹出多行文本输入框与身份信息字段。
- 扩展 `web/dialog.ts` 支持 `textarea` 输入类型，并补充样式。
- 在 `src/dataSource.ts` 实现 `prepareRewriteCommit` 与 `rewriteCommit`，处理 `git commit --amend` 与 `git rebase --exec` 场景。
- 同步更新 `src/gitGraphView.ts`、`src/gitGraphPanelView.ts`、`src/types.ts` 以传递新消息。
- 跑通 `npm run lint -- --fix`、`npm run compile`、`npm run package`。

## 待解决问题
- 暂无。

## 影响范围
- Git Graph 提交上下文菜单行为、对话框组件、后端 Git 操作封装。

## 验收建议
- 在视图中选中 HEAD 与历史提交，验证“修改提交信息”对话框可以打开、填入后能刷新视图。
- 测试仅修改消息、仅修改作者/提交者及同时修改的组合。
- 验证失败场景（例如工作区存在未提交改动）能正确反馈 Git 错误。

## 下一步计划
- 观测用户反馈，必要时补充对多父提交的特殊提示或限制说明。

## 开发与构建（备忘）
- 执行 `npm run lint -- --fix`、`npm run compile`、`npm run package`。
